# Go / No-Go Assessment: Paying Customers — 2026-07-02

**Repository:** `/Users/aryansingh/Documents/Claude/Projects/headroom` · `main` @ `3c515c10`
**Method:** 4 parallel specialist lanes (A: commercial, B: customer-facing, C: legal/compliance, D: marketing) + 2 prior engineering-assurance lanes reconciled into one verdict.
**Live probes performed during this session:** `curl https://pitchtoship.com/api/billing/checkout` (400), `curl https://pitchtoship.com` (200, different company), `curl -s -m 3 http://127.0.0.1:8787/livez` (200, healthy v0.30.0), `dig cutctx.dev` (NXDOMAIN).

---

## Bottom Line: NO-GO on 2 of 3 paying-customer scenarios. CONDITIONAL GO on the third.

### 2026-07-02 Verification Addendum

- Verified after this report that the stale Docker-native install links in `wiki/getting-started.md` and `wiki/quickstart.md` were real and are now fixed to the canonical `cutctx/cutctx` GitHub path.
- Verified after this report that the audit-chain "HMAC SHA-256" finding is real in the Python source path (`cutctx_ee/audit/store.py` still implements a plain SHA-256 secret-prefix chain while the docstring and docs describe HMAC), but this checkout also contains compiled `cutctx_ee/audit/store*.so` modules that shadow the source path at runtime. A safe runtime fix therefore needs a rebuild-aware EE change, not just a source edit.

| Scenario | Verdict | Why |
|---|---|---|
| **A. Design-partner pilot** (1-5 named accounts, founder-led, invoice-paid, $18-42K/yr) | ✅ **CONDITIONAL GO** | Tech works (live proxy healthy, 5/5 criticals fixed). Sales artifacts are calibrated. ~6 pre-pilot fixes are non-negotiable. |
| **B. Public self-serve** (open signups, public docs, no founder in the loop) | ❌ **NO-GO** | No working payment path. No live website. Pricing contradicts itself by 30×. Brand-name split. 4.8% median compression not disclosed. README hero says "HEADROOM." |
| **C. Enterprise sales** ($60-150K+/yr contracts with formal procurement + security review) | ❌ **NO-GO** | No SOC 2 report. No pentest report. No CAIQ/SIG. "HMAC" is actually plain SHA256. Single global admin key. No SAML. Backup covers 3 of 13+ stores. |

**If the user picks Scenario A, the work to unblock it is a 1-2 week "Stripe + contact-domain + brand-consistency" sprint that touches ~25 files. See P0 list below.**

---

## The 8 Findings That Block Any Paying Customer

These are the 8 facts that, if a paying customer encountered them on day 1, would stop the deal:

| # | Fact | Impact | Source |
|---|---|---|---|
| 1 | **PitchToShip billing API is dead upstream** — `curl https://pitchtoship.com/api/billing/checkout` returns HTTP 400 with `"Missing plan configuration. Set STRIPE_PRICE_STARTER..."`. The fallback URL is a marketing page, not Stripe. | **No customer can pay through the documented flow.** | Lane A · `cutctx/billing.py:65-89`; `cutctx_ee/billing/__init__.py:68-85`; live probe |
| 2 | **`cutctx.dev` is NXDOMAIN** — every contact email in the repo (`hello@`, `licenses@`, `security@`, `privacy@`, `conduct@`) bounces. The website a customer's lawyer will visit is offline. | **All license deliveries, support requests, security disclosures fail.** | Lane A · live `dig` + `nslookup`; 28+ files reference `cutctx.dev` |
| 3 | **PitchToShip is owned by a different company** ("A small studio building products that are close enough to matter"). A customer who follows the cutctx→PitchToShip trail ends up on an unrelated studio's site. | **This is not a Cutctx-owned portal.** The whole payment flow is a broken third-party stub. | Lane A · `curl https://pitchtoship.com` |
| 4 | **Two public pricing pages show wrong prices.** `docs/pricing.html:110` and `docs/enterprise.html:500` both say **Team = $49/month**. The canonical sheet says **$1,500/month**. A 30× pricing error. | **A buyer who Googles "cutctx pricing" finds the $49 number and will quote it in the sales call.** | Lane D |
| 5 | **Brand-name split is published.** README hero ASCII art literally draws "HEADROOM" but the product is "Cutctx." Three brands in active use (Cutctx / CutCtx / Headroom). `cutctx_ee/LICENSE:7` says "Payzli Inc." but `cutctx_ee/__init__.py:2` says "Cutctx Labs." | **First 30 seconds of any enterprise review will flag the entity mismatch.** | Lane C + D |
| 6 | **The "HMAC SHA-256" audit chain is plain SHA-256.** `cutctx_ee/audit/store.py:85` uses `hashlib.sha256()` with the secret concatenated, not `hmac.new()`. The docstring claims HMAC. An auditor or security reviewer reading the code will catch this. | **SOC 2 CC7.1 evidence fails on first read.** A claim of cryptographic tamper-evidence is technically false. | Lane C |
| 7 | **Backup covers 3 of 13+ SQLite stores.** `k8s/backup-cronjob.yaml` only backs up `cutctx_memory.db`, `spend_ledger.db`, `audit.db`. RBAC, billing, webhook DLQ, team memory, knowledge graph, vector embeddings are NOT backed up. No verification step. | **A disk failure on a paid-tier customer loses billing state, RBAC, and audit trail. Non-recoverable.** | Lane B |
| 8 | **No public penetration test, no SOC 2 report, no CAIQ/SIG.** The repo claims annual pentest under NDA but no report exists. SOC 2 is "in preparation, target Q4 2026." No CAIQ-v4 or SIG-Lite pre-filled. | **Any procurement gate will fail at the security questionnaire step.** | Lane C |

---

## Per-Scenario Detail

### Scenario A: Design-Partner Pilot (1-5 named accounts, founder-led)

**Verdict: CONDITIONAL GO.** A single design partner at $18-42K/yr, invoiced outside the system (Net 30 wire transfer, per `artifacts/pricing-sheet.md:202-204`), with founder-led support, is the path the artifacts were written for. `artifacts/pitchdeck.md:5`, `audit/final-verdict.md:172`, and `audit/production-readiness-2026-07-02.md:15` all agree on this scenario.

#### What's already good (no work needed)

- Live proxy is healthy (v0.30.0, `/livez` returns 200, rust_core loaded, all checks healthy)
- 5/5 CRITICAL security items are fixed (residency verify, DSR, residency route auth, Stripe tier, EgressEnforcer)
- Sales collateral: `artifacts/pitchdeck.md`, `artifacts/roi-calculator.md`, `artifacts/security-one-pager.md`, `artifacts/pilot-success-metrics.md` — all calibrated, all on-message
- 4.8% median compression is disclosed in the pitchdeck (small-font caveat at line 50-51) — internally honest
- Air-gap mode works: `CUTCTX_OFFLINE_MODE=1` + signed offline license keys via Ed25519 (`cutctx_ee/billing/pitchtoship_client.py:198-350`)
- DSR endpoints exist and are honest about the spend-ledger narrow scope
- Pilot acceptance criteria are documented (`artifacts/pilot-success-metrics.md`)

#### What MUST be fixed before the first invoice (1-2 weeks)

| # | Fix | Effort | File:line |
|---|---|---:|---|
| A1 | **Skip PitchToShip — wire Stripe Checkout directly into the proxy.** The webhook handler is already there; just add `stripe.checkout.Session.create()` to `cutctx/cli/billing.py:checkout`. | 3-4 days | `cutctx/billing.py:65-89`, `cutctx_ee/billing/stripe_webhook.py:31-35` |
| A2 | **Register `cutctx.dev` (or repoint to a working domain).** Add a 1-page landing with Privacy/Terms/Security. Add `/.well-known/security.txt`. Update 28+ file references. | 1-2 days | `TERMS.md:76`, `SECURITY.md:19`, `DPA_TEMPLATE.md:79`, `ENTERPRISE.md:161-162`, `artifacts/security-one-pager.md:60-64` |
| A3 | **Fix brand-entity chain.** Update `cutctx_ee/**/__init__.py` and `cutctx_ee/billing/*.py` file headers from "Cutctx Labs" to "Payzli Inc. (operating as Cutctx Labs)". | 1 hour | `cutctx_ee/__init__.py:1-2`, `cutctx_ee/billing/stripe_webhook.py:2` |
| A4 | **Fix the 2 stale pricing pages.** Update `docs/pricing.html:110` and `docs/enterprise.html:500` to show $1,500/mo Team. | 1 hour | `docs/pricing.html`, `docs/enterprise.html` |
| A5 | **Fix README hero ASCII art.** Remove "HEADROOM" or replace with a real Cutctx wordmark. | 30 min | `README.md:1-9` |
| A6 | **Fix the "HMAC" audit chain misnomer.** Either change `cutctx_ee/audit/store.py:85` to actually use `hmac.new()` (the honest fix), or update all docs that claim HMAC. | 1 day | `cutctx_ee/audit/store.py:83-94`, `docs/audit-compliance.md:6-8`, `docs/data-residency.md:62-67` |
| A7 | **Reconcile the SOC 2 roadmap to match reality.** MFA is done (un-check), SAML is honestly Partial, automated-backups row is reconciled with `SOC2_CONTROLS.md:58`. | 2 hours | `gtm/soc2-roadmap.md:32-33, 51, 62, 87` |
| A8 | **Fix the install one-liner.** Replace `chopratejas/cutctx` with `cutctx/cutctx` in `wiki/getting-started.md:32`, `wiki/quickstart.md:31`. | 30 min | `wiki/getting-started.md:32`, `wiki/quickstart.md:31` |

**Total: 8-10 working days. The first invoice can land in week 3.**

#### What's disclosed and acceptable to a design partner

- No SOC 2 report (defer to `gtm/soc2-roadmap.md` and the pre-filled vendor questionnaire)
- "In preparation" backup scope (3 of 13+ stores backed up; document the gap, set expectation)
- "HMAC" is plain SHA-256 (only relevant if their security team reads the EE source; fix or document)
- 4.8% median compression (already disclosed honestly in the pitchdeck)
- No SAML SSO (defer; OIDC works for most IdPs)
- Brand-name split (fix before A5, disclose otherwise)
- PitchToShip dependency (fix before A1; invoice outside the system until then)

**Recommended design-partner profile:** A Series A-B AI agent company, 5-20 engineers, founder-led, with an LLM spend of $10-25K/mo, who needs the 60-95% eligible-workload compression AND has an OIDC-capable IdP. The pitchdeck's call to "ship to pilot customers, defer marketing launch" applies here.

---

### Scenario B: Public Self-Serve (open signups, public docs, no founder in the loop)

**Verdict: NO-GO.**

This requires all the design-partner work PLUS a public marketing surface that the repo does not have.

| # | What's missing | Why it's a NO-GO | Source |
|---|---|---|---|
| B1 | **No live website.** `cutctx.dev` is NXDOMAIN. | First-click failure. | Lane A + D |
| B2 | **No working self-serve payment.** PitchToShip is dead; no direct-Stripe path. | Customer can't pay. | Lane A |
| B3 | **Dashboard has no billing/subscription page.** No `Billing.jsx`, no `Pricing.jsx`, no `Subscription.jsx`. | Customer can't self-serve upgrade/downgrade. | Lane A · `dashboard/src/pages/` |
| B4 | **3 different Team prices in 3 different files** ($49 in `docs/pricing.html`, $1,500 in `artifacts/license-portal.html`, $1,500 in `marketing/roi-calculator/index.html`). | Customer-facing contradiction. | Lane A + D |
| B5 | **No real case studies, no community size, no Discord member count, no GitHub stars shown.** | Zero social proof. | Lane D |
| B6 | **4.8% median compression not in any public surface** (only the pitchdeck's small-font caveat). README hero says "60-95% fewer tokens" without the median. | A technical buyer who reads both will see the gap. | Lane D |
| B7 | **Brand-name split (Cutctx / CutCtx / Headroom).** README hero literally says "HEADROOM." | Looks like a fork or a rebrand in progress. | Lane D |
| B8 | **OG image route hardcodes "My App" placeholder.** | Every shared link to docs renders with "My App" on the social card. | Lane D · `docs/app/og/docs/[...slug]/route.tsx:14` |
| B9 | **Blog links to wrong domain (`cutctx.sh`).** 5 blog files, all stale. | Every blog CTA is a 404. | Lane D |
| B10 | **No status page, no public `/health`, no community channels sized for self-serve.** | No way to know if the service is up. | Lane D |
| B11 | **No "we're in pilot" framing on public surfaces.** README and wiki read as if cutctx is launched. | Misleading. | Lane D |
| B12 | **No public changelog with "what's next" / no public roadmap.** | Self-serve buyers need to know what's coming. | Lane D |

**The gap from design-partner (Scenario A) to public self-serve is much larger than from "not paying customers" to design-partner.** It's a 1-2 month sprint, not a 1-2 week sprint. Most of it is marketing surface work (real landing page, real pricing page, real blog, real status page, real social proof) — none of which is engineering work.

**DO NOT pursue Scenario B until at least 5 design partners are signed and producing public testimonials.**

---

### Scenario C: Enterprise Sales ($60-150K+/yr with formal procurement)

**Verdict: NO-GO.**

This scenario has the most demanding requirements, and the repo is the least ready for it.

| # | What's missing | Why it's a NO-GO | Source |
|---|---|---|---|
| C1 | **No SOC 2 report.** Status: "in preparation, target Q4 2026." No auditor engaged. | **Any enterprise procurement team will require SOC 2 as a vendor gate.** This is the single biggest blocker. | Lane C · `SOC2_CONTROLS.md:5`, `gtm/soc2-roadmap.md:96` |
| C2 | **No public penetration test report.** `SECURITY_POLICY.md:222-236` claims annual pentest under NDA but no report exists. | Procurement asks for the report. There's nothing to point them at. | Lane C |
| C3 | **No CAIQ-v4 or SIG-Lite pre-filled.** The 40-question custom `VENDOR_SECURITY_QUESTIONNAIRE.md` is well-written but not in a GRC-ingestible format. | Procurement team can't load the answers into their tooling. | Lane C |
| C4 | **No SAML SSO.** OIDC works for most IdPs but some enterprise IdPs (legacy ADFS, government) are SAML-only. | Blocks some enterprise buyers. | Lane C · `cutctx_ee/sso.py` |
| C5 | **Single global admin API key.** No per-user keys, no per-service keys, no scoped keys, no rotation endpoint, no expiry. | Doesn't match a multi-admin enterprise model. | Lane C · `cutctx/proxy/server.py:2836-2856` |
| C6 | **MFA is enrollment-gated, not mandatory.** An enterprise buyer expecting "MFA mandatory for all admins" cannot enforce it. | Compliance gap. | Lane C · `cutctx/proxy/server.py:2916-2919` |
| C7 | **No PGP key for `security@cutctx.dev`.** No `/.well-known/security.txt`. | High-assurance enterprise + government buyers require PGP-encrypted vulnerability disclosure. | Lane C |
| C8 | **No bug bounty program.** `VENDOR_SECURITY_QUESTIONNAIRE.md:145` honestly says "private responsible disclosure" only. | Some enterprise procurement teams require bug-bounty evidence. | Lane C |
| C9 | **Brand-name mismatch in EE LICENSE.** `cutctx_ee/LICENSE:7` "Payzli Inc." vs `cutctx_ee/__init__.py:2` "Cutctx Labs." | Counsel will catch in the first 30 minutes of legal review. | Lane C |
| C10 | **`cutctx_ee/LICENSE` and root `LICENSE-COMMERCIAL` are divergent** (different contact URLs at the end: PitchToShip vs. hello@cutctx.dev). | Counsel will catch this too. | Lane C |
| C11 | **Backup covers 3 of 13+ stores.** No verification. RBAC, billing, webhook DLQ, team memory, knowledge graph, vector embeddings are not backed up. | A paid-tier customer loses billing state on a disk failure. Non-recoverable. | Lane B |
| C12 | **No real uptime SLA.** `SLA.md` defines support response times only, no availability target, no credit structure. | A customer paying $60-150K/yr expects a real SLA. | Lane B · `SLA.md:1-46` |
| C13 | **No customer-facing DR runbook.** Internal `OPERATIONAL_RUNBOOK.md` is operator-focused and partially stale. | Customer can't self-recover. | Lane B |
| C14 | **No public trust center.** No CAIQ, no SIG, no SOC 2 report, no pen test report. | Trust center is the standard enterprise due-diligence entry point. | Lane C |
| C15 | **2 real competitive gaps that enterprise buyers will notice.** (a) No verification / hallucination guard (vs Entroly WITNESS, AUROC 0.844). (b) No read-side intelligence (vs LeanCTX 10 read modes). | A CISO doing competitive diligence will see the gap. | Lane D · `audit/product-manager-report.md:77, 85` |
| C16 | **No `customer.subscription.created` handler** in `cutctx_ee/billing/stripe_webhook.py`. Only `.deleted` and `.updated` are stubbed. | Would create orphaned subscriptions on payment-method changes. | Lane A · `cutctx_ee/billing/stripe_webhook.py:149-201` |
| C17 | **No test coverage for the EE enterprise surface** (SSO, RBAC, SCIM, audit, retention, fleet, DSR, residency, abuse, secrets). `cutctx_ee/tests/` has 3 small files. | "How do you regression-test these features?" is an unanswerable question. | Lane C |

**The gap from design-partner (Scenario A) to enterprise (Scenario C) is 2-3 months of focused work.** It's primarily:
- Engagements: SOC 2 auditor, pentest firm, legal review of all templates
- Engineering: SAML SSO, multi-key admin, MFA-mandatory, audit chain HMAC fix, backup expansion to all 13+ stores with verification
- Documentation: CAIQ-v4 / SIG-Lite export, public trust center, real DR runbook, real SLA
- Time: SOC 2 Type I takes 3-6 months minimum, even with an aggressive auditor

**Recommended Scenario C entry point:** A design partner with $60K+/yr spend who agrees to a 90-day custom MSA + security addendum, in exchange for a price concession. Use the early revenue to fund the SOC 2 audit and pentest.

---

## The 30-File Sprint to Unblock Scenario A

If the user picks Scenario A, the specific files to touch (no code-engineering work, all commercial/legal/marketing surface):

**Stripe / payment (A1):**
1. `cutctx/cli/billing.py:65-89` — add `stripe.checkout.Session.create()` direct path
2. `cutctx_ee/billing/__init__.py:68-85` — fall back to direct-Stripe when PitchToShip returns 400
3. `cutctx_ee/billing/stripe_webhook.py:149-201` — add `customer.subscription.created` handler

**Domain / contact (A2):**
4. Register `cutctx.dev` or repoint to a working domain
5. Add 1-page landing with Privacy/Terms/Security/Contact
6. Add `/.well-known/security.txt` with PGP key
7. `TERMS.md:76` — `hello@cutctx.dev` → working address
8. `SECURITY.md:19` — `security@cutctx.dev` → working address
9. `DPA_TEMPLATE.md:79` — `cutctx.dev/sub-processors` → working URL
10. `ENTERPRISE.md:161-162` — `hello@cutctx.dev` → working address
11. `CODE_OF_CONDUCT.md:63` — `conduct@cutctx.dev` → working address
12. `artifacts/security-one-pager.md:60-64` — broken path fix

**Brand / entity (A3, A5):**
13. `cutctx_ee/__init__.py:1-2` — "Cutctx Labs" → "Payzli Inc. (operating as Cutctx Labs)"
14. `cutctx_ee/billing/stripe_webhook.py:2` — same
15. `cutctx_ee/billing/seats.py:2` — same
16. `cutctx_ee/billing/trial.py:2` — same
17. `cutctx_ee/billing/entitlements.py:2` — same
18. `cutctx_ee/billing/__init__.py:2` — same
19. `cutctx_ee/LICENSE:7, 143` — make consistent with root `LICENSE-COMMERCIAL:142`
20. `README.md:1-9` — remove or fix "HEADROOM" ASCII art

**Pricing (A4):**
21. `docs/pricing.html:110` — Team $49 → $1,500
22. `docs/enterprise.html:500` — Team $49 → $1,500
23. `cutctx_ee/LICENSE:143` — PitchToShip URL → `hello@cutctx.dev` (or working domain)

**Cryptographic honesty (A6):**
24. `cutctx_ee/audit/store.py:83-94` — change `hashlib.sha256()` to `hmac.new(self.secret_key, ...)` (the honest cryptographic fix)
25. `docs/audit-compliance.md:6-8` — already accurate if store.py is fixed
26. `docs/data-residency.md:62-67` — already accurate if store.py is fixed
27. `SOC2_CONTROLS.md:25` — already accurate if store.py is fixed

**SOC 2 roadmap sync (A7):**
28. `gtm/soc2-roadmap.md:32-33, 51, 62, 87` — un-check MFA, reconcile backups row with `SOC2_CONTROLS.md:58`

**Install one-liner (A8):**
29. `wiki/getting-started.md:32` — `chopratejas/cutctx` → `cutctx/cutctx`
30. `wiki/quickstart.md:31` — `chopratejas/cutctx` → `cutctx/cutctx`

**30 files. 1-2 weeks of focused work. The first invoice lands in week 3.**

---

## Recommended Path Forward

**Week 1-2:** 30-file sprint above. Result: Scenario A (design-partner pilot) is GO.

**Week 3-6:** First design partner signs, onboards, runs the 14-day pilot. Use the design partner's feedback to refine the pitchdeck and pilot-success-metrics doc. Generate one anonymized case study.

**Week 7-12:** 3-5 design partners. $90-300K ARR. Use the early revenue to fund:
- SOC 2 audit engagement (90-day sprint, ~$45-70K)
- Third-party penetration test (~$15-25K)
- Legal review of all templates by qualified counsel (~$5-10K)
- SAML SSO implementation (2-3 weeks engineering)
- Multi-key admin API (1-2 weeks engineering)
- Real uptime SLA, real backup of all 13+ stores, real DR runbook

**Week 13-26:** Scenario C (enterprise sales) becomes GO. Scenario B (public self-serve) becomes GO after the trust artifacts ship.

**Net: 6 months from today to a fully-launchable product across all 3 scenarios. The engineering work is mostly done (76/100 production-readiness). The remaining 24 points are commercial/legal/marketing surface work, not code work.**

---

## Sources

This assessment consolidated findings from 4 new parallel lanes + 2 prior engineering lanes + 14 prior audit files (cited in `audit/production-readiness-2026-07-02.md`).

**Live probes performed during this session:**
- `curl https://pitchtoship.com/api/billing/checkout` — returns HTTP 400 "Missing plan configuration"
- `curl https://pitchtoship.com` — returns 200, meta description: "A small studio building products that are close enough to matter"
- `curl -s -m 3 http://127.0.0.1:8787/livez` — returns 200, `{"status":"healthy","ready":true,"version":"0.30.0","rust_core":"loaded"}`
- `dig cutctx.dev` / `nslookup cutctx.dev` — NXDOMAIN

**Lane outputs:**
- Lane A (Commercial surface): terminal in chat
- Lane B (Customer-facing): terminal in chat
- Lane C (Legal/Compliance/Enterprise): terminal in chat
- Lane D (Competitive/Marketing): saved to `tool_f22027382001Ng9GUGcaKllYSH`

**Prior session artifacts referenced:**
- `audit/production-readiness-2026-07-02.md` (76/100 engineering score, 5/5 criticals fixed)
- `audit/final-verdict.md` (pilot release ready)
- `artifacts/pitchdeck.md` (14-slide sales deck)
- `artifacts/50-client-outreach-plan.md` (60-day SDR plan)
- `artifacts/roi-calculator.md` (3 hypothetical case studies)
- `artifacts/security-one-pager.md`
- `artifacts/pilot-success-metrics.md`
- `artifacts/pricing-sheet.md` (canonical tier prices)

---

**Final recommendation: pursue Scenario A. The 30-file sprint is bounded, low-risk, and unlocks the first dollar. Defer Scenario B and C until the design-partner program produces real customers and the SOC 2 / pentest / legal budget can be funded from early revenue.**
