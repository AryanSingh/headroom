# Publication Fonts

The publication source uses Calibri for prose and Courier New for code. Both are
available in the pinned local publication environment. If a build host lacks a
font, the builder may substitute the platform default, but that build is not a
release candidate until the rendered-page ledger records the substitution and a
reviewer approves or corrects it.
