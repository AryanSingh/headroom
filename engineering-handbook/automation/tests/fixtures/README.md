# Validator fixtures

The automated tests build isolated handbook and executable-example fixtures in
temporary directories. This directory is reserved for binary or multi-file
fixtures that cannot be expressed clearly by the fixture builders.
