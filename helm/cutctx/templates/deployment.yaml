apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cutctx.fullname" . }}
  labels:
    {{- include "cutctx.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "cutctx.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "cutctx.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "cutctx.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.proxy.port }}
              protocol: TCP
          env:
            - name: CUTCTX_HOST
              value: "0.0.0.0"
            - name: CUTCTX_PORT
              value: "{{ .Values.proxy.port }}"
            - name: CUTCTX_PROXY_COMPRESSION
              value: "{{ .Values.proxy.compression }}"
            - name: CUTCTX_PROXY_COMPRESSION_MODE
              value: "{{ .Values.proxy.compressionMode }}"
            - name: CUTCTX_BACKEND
              value: "{{ .Values.proxy.backend }}"
            - name: CUTCTX_MODE
              value: "{{ .Values.proxy.mode }}"
            - name: CUTCTX_MAX_BODY_MB
              value: "{{ .Values.proxy.maxBodyMb }}"
            - name: CUTCTX_AUDIT_DISABLED
              value: "{{ if .Values.audit.enabled }}0{{ else }}1{{ end }}"
            {{- if .Values.proxy.upstream }}
            - name: CUTCTX_PROXY_UPSTREAM
              value: "{{ .Values.proxy.upstream }}"
            {{- end }}
            {{- if .Values.enterprise.adminApiKey }}
            - name: CUTCTX_ADMIN_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "cutctx.fullname" . }}-secrets
                  key: admin-api-key
            {{- end }}
            {{- if .Values.enterprise.proxyApiKey }}
            - name: CUTCTX_PROXY_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "cutctx.fullname" . }}-secrets
                  key: proxy-api-key
            {{- end }}
            {{- if .Values.enterprise.clientApiKey }}
            - name: CUTCTX_CLIENT_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "cutctx.fullname" . }}-secrets
                  key: client-api-key
            {{- end }}
            {{- if .Values.enterprise.entitlementTier }}
            - name: CUTCTX_ENTITLEMENT_TIER
              value: "{{ .Values.enterprise.entitlementTier }}"
            {{- end }}
            {{- if .Values.sso.enabled }}
            - name: CUTCTX_SSO_PROVIDER_TYPE
              value: "{{ .Values.sso.providerType }}"
            - name: CUTCTX_SSO_DISCOVERY_URL
              value: "{{ .Values.sso.discoveryUrl }}"
            - name: CUTCTX_SSO_JWKS_URI
              value: "{{ .Values.sso.jwksUri }}"
            - name: CUTCTX_SSO_ISSUER
              value: "{{ .Values.sso.issuer }}"
            - name: CUTCTX_SSO_AUDIENCE
              value: "{{ .Values.sso.audience }}"
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          startupProbe:
            {{- toYaml .Values.startupProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.persistence.enabled }}
            - name: data
              mountPath: /home/nonroot/.cutctx
            {{- end }}
            - name: tmp
              mountPath: /tmp
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        {{- if .Values.persistence.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "cutctx.fullname" . }}-pvc
        {{- end }}
        # The image runs with a read-only root filesystem. Python, TLS, and
        # optional provider SDKs need a writable temporary directory.
        - name: tmp
          emptyDir:
            sizeLimit: 64Mi
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
