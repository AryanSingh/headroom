apiVersion: v1
kind: Service
metadata:
  name: {{ include "cutctx.fullname" . }}
  labels:
    {{- include "cutctx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cutctx.selectorLabels" . | nindent 4 }}
