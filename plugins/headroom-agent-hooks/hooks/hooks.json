{
  "description": "CutCtx plugin hooks — ensure the local CutCtx runtime is available for initialized agents.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "cutctx init hook ensure",
            "timeout": 15
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "cutctx init hook ensure",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
