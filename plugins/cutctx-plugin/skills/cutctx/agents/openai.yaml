interface:
  display_name: "Cutctx"
  short_description: "Automatic context compression for Codex"
  default_prompt: "Use $cutctx automatically for large outputs, preserve retrievable hashes, and report token savings honestly."

policy:
  allow_implicit_invocation: true
