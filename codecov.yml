codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

ignore:
  - "tests/**"
  - "scripts/tests/**"
  - ".github/**"
  - ".claude-plugin/**"
  - "plugins/cutctx-agent-hooks/.claude-plugin/**"
  - "plugins/cutctx-agent-hooks/.github/**"
