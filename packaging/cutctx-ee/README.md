# cutctx-ee (proprietary)

Commercial ("enterprise edition") components for Cutctx. **Proprietary — All Rights Reserved.**
Licensed under the Cutctx Commercial License (`/LICENSE-COMMERCIAL`), **not** Apache-2.0.

The package source lives at the repo root in `cutctx_ee/`. The open-source `cutctx`
client exposes Apache-2.0 import shims (`cutctx/entitlements.py`, `cutctx/billing/`, …)
that re-export these implementations when this distribution is installed and the
corresponding license entitlement is present.

See `/LICENSING.md` for the authoritative open-core boundary.
